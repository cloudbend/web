version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd next
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /next/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
