version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd next
        - npm i --force
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: next/.next
    files:
      - '**/*'
  cache:
    paths:
      - next/node_modules/**/*
